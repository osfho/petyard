<%= form_for(@mod_message) do |f| %>
  <% if @mod_message.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@mod_message.errors.count, "error") %> prohibited this mod_message from being saved:</h2>

      <ul>
      <% @mod_message.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :body %><br />
    <%= f.text_area :body %>
  </div>
  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :post_id %><br />
    <%= f.number_field :post_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
