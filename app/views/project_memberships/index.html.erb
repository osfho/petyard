<h1>Listing project_memberships</h1>

<table>
  <tr>
    <th>User</th>
    <th>Projec idinteger</th>
    <th>Role</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @project_memberships.each do |project_membership| %>
  <tr>
    <td><%= project_membership.user_id %></td>
    <td><%= project_membership.projec_idinteger %></td>
    <td><%= project_membership.role %></td>
    <td><%= link_to 'Show', project_membership %></td>
    <td><%= link_to 'Edit', edit_project_membership_path(project_membership) %></td>
    <td><%= link_to 'Destroy', project_membership, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Project membership', new_project_membership_path %>
