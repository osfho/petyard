<p id="notice"><%= notice %></p>

<p>
  <b>Title:</b>
  <%= @rank.title %><br />
  <%= image_tag @rank.icon.url %>
</p>


<%= link_to 'Edit', edit_rank_path(@rank) %> |
<%= link_to 'Back', ranks_path %>
